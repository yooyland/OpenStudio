# YooY AI Studio 12.6.0 — Auto Studio Routing + Remix Continuity

## Changes

- Home composer **Studio 자동 선택** now routes with existing prompt heuristics (`YooYHomeIntent`) plus attachment context. No second AI Router.
- `+` menu uses Import Engine (image/file) and Translator website extract (URL). Unified Home attachment chip.
- Remix / 따라 만들기 keeps Gallery metadata, prompt, and a removable Studio context banner.
- Ambiguous prompts show inline Image / Video / Writing / 직접 선택 chips.

## Install

1. Upload `yooy-ai-studio-12.6.0-auto-studio-remix.zip` (or update the plugin folder).
2. Activate or keep active. PHP 7.4+, WordPress 6.x.

## Requirements

- PHP 7.4
- WordPress 6.x
