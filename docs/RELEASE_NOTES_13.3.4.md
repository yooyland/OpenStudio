# Release Notes — 13.3.4

**YooY AI Studio** — Profile avatar fallback hotfix

## Changes

- MY menu avatar falls back to initials when the image URL is missing or fails to load
- Broken `<img>` is removed on error (no broken-image icon)
- Circular avatar keeps dark background + subtle gold border
- Accessible label: `{display_name} 프로필`

## Install

Hard-refresh Creator OS assets (`?ver=13.3.4`).

## Requirements

- WordPress 6.x
- PHP 7.4+
